APP_ENV="development"
PORT=8000
DATABASE_URL="keep-me-secret"
COOKIE_SECRET="replace-with-strong-secret"

STRIPE_SECRET_KEY="sk_test_replace_me"
STRIPE_PUBLISHABLE_KEY="pk_test_replace_me"
STRIPE_WEBHOOK_SECRET="whsec_replace_me"

DEPLOY_PROJECT="deno-hono-shop"

# Optional: prefix for product images (e.g. https://cdn.example.com)
IMAGE_BASE_URL=""

# DATABASE_URL is required; use your Neon/Postgres connection string.
# DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DB"
#
# Deno Deploy dashboard tip: paste raw values only (no quotes / no `KEY=` prefix).
